trait ApplicationAbstract {
    fn new() -> Self;
    fn init();
    fn update();

    fn populate_main();
    fn populate_settings();
}

trait PopulateSettings {
    // TODO etc.
    fn setting_create() {

    }
}